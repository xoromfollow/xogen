# Utility functions for GenLayer Intelligent Contracts

def validate_positive_amount(amount: int) -> bool:
    """Simple helper to validate amounts"""
    return amount > 0

# Add more helpers here as you build (LLM prompt templates, data validation, etc.)
